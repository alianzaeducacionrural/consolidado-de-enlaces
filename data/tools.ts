/**
 * Este archivo quedó como compatibilidad. Los datos de enriquecimiento ahora
 * viven en `data/overrides.json` (editable a mano o desde /panel) y los datos
 * de usuarios/cuentas en `data/accounts.local.json` (fuera de Git).
 *
 * Ver `lib/store.ts` para la lectura/escritura.
 */
export {};
